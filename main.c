#include <stdio.h>

int main(int argc, char *argv[]) 
{
    int str[5];
    int i;
    
	i=0;
	
    while(1){
	    
        if (atr[i] < 0 && str[-str[i] - 1] >= 0) {
		printf("%d\n", str[-str[i] - 1]);
	}

	else if(str[i] < 0 && str[-str[i] - 1] < 0) {
		printf("%d\n", str[-str[i]]);
	}
	    
	    else{
	    break;
	    }
	    
    }

    return 0;
}
